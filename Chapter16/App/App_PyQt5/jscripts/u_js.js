function RZfunc(width) {                      // Javascript function retreive
    for (i=0; i=width; i++) {                 // the parameter - width.
        return i;                             // Returns the number from 
    };                                        // from range 0 - width, with
}                                             // "for" loop generated.