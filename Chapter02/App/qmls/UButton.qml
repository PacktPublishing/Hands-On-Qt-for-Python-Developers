import QtQuick.Controls 2.2            // Import of the Controls for button.

Button {                               // Declaration of the standard Button.
    flat: true                         // This button without borders - flat.
    highlighted: true                  // Button will be highlighted when
}                                      // clicked or hovered with mouse.